package zendeskintone

import "testing"

func TestNewRecord(t *testing.T) {
	// body := []byte(`{"name":"hello"}`)
	// r, err := newRecord(body)
	// if err != nil {
	// 	t.Error(err)
	// }
	// t.Log(r.Fields["kintone_app_id"])
}
